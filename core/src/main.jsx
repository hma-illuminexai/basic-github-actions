import React from 'react'
import ReactDOM from 'react-dom/client'

import GreetingComponent from './Component.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GreetingComponent />
  </React.StrictMode>,
)
